import {h, Component} from 'preact';

export default class Menu extends Component {
    /**
     * Preact render function
     *
     * @returns {*}
     */
    render() {
        return (
            <div>
                Menu
            </div>
        );
    }
}
