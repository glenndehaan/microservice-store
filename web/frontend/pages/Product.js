import { h, Component } from 'preact';

export default class Product extends Component {
    /**
     * Preact render function
     *
     * @returns {*}
     */
    render() {
        return (
            <main>
                Product
            </main>
        );
    }
}
